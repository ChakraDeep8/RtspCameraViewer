<Application x:Class="RtspCameraViewer.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <SolidColorBrush x:Key="AppBackground" Color="#1E1E24"/>
            <SolidColorBrush x:Key="TileBackground" Color="#2A2A33"/>
            <SolidColorBrush x:Key="AccentBrush" Color="#3D8BFF"/>
            <SolidColorBrush x:Key="TextPrimary" Color="#F1F1F4"/>
            <SolidColorBrush x:Key="TextSecondary" Color="#9A9AA5"/>
            <SolidColorBrush x:Key="OkBrush" Color="#3ECF6B"/>
            <SolidColorBrush x:Key="ErrorBrush" Color="#E5484D"/>
            <SolidColorBrush x:Key="WarnBrush" Color="#F5A623"/>

            <Style TargetType="Button" x:Key="ToolButton">
                <Setter Property="Background" Value="{StaticResource TileBackground}"/>
                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                <Setter Property="Padding" Value="14,8"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!--
                Square icon buttons. ToolButton's 14,8 padding is right for a text button but
                bigger than a 24px icon button IS, so the glyph was laid out outside the button
                and clipped - the toolbar read as a row of half-symbols. Padding 0 plus MinWidth 0
                lets the explicit Width/Height be the whole button and centres the glyph in it.
            -->
            <Style TargetType="Button" x:Key="IconButton" BasedOn="{StaticResource ToolButton}">
                <Setter Property="Padding" Value="0"/>
                <Setter Property="MinWidth" Value="0"/>
                <Setter Property="MinHeight" Value="0"/>
            </Style>

            <Style TargetType="TextBox" x:Key="InputBox">
                <Setter Property="Background" Value="#33333D"/>
                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                <Setter Property="Padding" Value="8,6"/>
                <Setter Property="BorderBrush" Value="#44444F"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CaretBrush" Value="White"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
